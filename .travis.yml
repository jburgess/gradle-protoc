language: bash

services:
  - docker

before_install:
  - docker build -t automatizer/gradle-protoc:latest .
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
  - docker push automatizer/gradle-protoc:latest
